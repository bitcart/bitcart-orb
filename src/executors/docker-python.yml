description: >
  Docker executor for regular python builds
parameters:
  version:
    description: "Python version"
    default: "3.9"
    type: string
docker:
  - image: cimg/python:<<parameters.version>>
