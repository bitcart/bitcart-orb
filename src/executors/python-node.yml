description: >
  Executor for python+node builds
parameters:
  version:
    description: "Python version"
    default: "3.11"
    type: string
docker:
  - image: cimg/python:<<parameters.version>>-node
